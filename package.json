{
  "name": "central-auth-broker",
  "version": "1.0.0",
  "private": true,
  "description": "Modular, extensible SaaS-token broker running as a Cloudflare Worker behind Cloudflare Access",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "pretest": "npm run validate:manifest",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250718.0",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.10"
  }
}
